services:
  - type: web
    name: bot-sports-empire
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ultra_minimal_app:app --host 0.0.0.0 --port $PORT
    rootDir: .
